keep track of the user preferences, as well as the preferences used in the last sync. That way, every sync you can compare the two and locate the diffs. If you see a class go from unincluded to included, nothign changes - the for loop logic stands where you check if the task list exists, if not create it. Then include all tasks. If it goes from included to unincluded, change the for loop logic to not include from that class. Then, delete all instances of tasks from that class from teh list, then delet eh list. 